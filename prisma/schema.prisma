generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model User {
  id        Int    @id @default(autoincrement())
  username  String @unique
  firstName String? 
  lastName  String? 
  password  String 
  Coloc     Coloc[]
}

model Coloc {
  id        Int    @id @default(autoincrement())
  name      String @unique
  users     User[]
}